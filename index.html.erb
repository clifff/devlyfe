<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" type="text/css" href="/css/bootstrap.css">
    <title>Dev Lyfe</title>
  </head>
  <body>
    <div class='container'>
      <header class='jumbotron subhead'>
        <h1>Dev Lyfe</h1>
        <p class='lead'>
          Links about stuff. Last updated at <%= Time.now.strftime("%I:%M%p %Z") %>
        </p>
      </header>
      <% @sites.each_slice(2) do |sites| %>
        <div class='row'>
          <% sites.each do |site| %>
            <div class='span6'>
              <h2>
                <a href='<%= site[:url] %>'><%= site[:name] %></a>
              </h2>
              <ul>
              <% site[:entries].each do |entry| %>
                <li>
                  <a href='<%= entry[:url] %>'><%= entry[:title] %></a>
                </li>
              <% end %>
              </ul>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </body>
</html>
